% !TEX root = ../main.tex
% !aim: we provide the necessary theoretical background for future chapters, critically examine the pieces of software and numerical methods employed in this work, and discuss the logistics of running this single-thread grid-based code as a multiprocessed code on a distributed cluster.

\customchapter{Theoretical Background}{A map is not the territory it represents, but, if correct, it has a similar structure to the territory, which accounts for its usefulness.}{Alfred Korzybski}\label{chap:theoretical_background}

\chapterEpigraph

\lettrine[lines=3]{I}{n this chapter, we provide a} brief account of the evolution of a star from a theoretical perspective, from its formation to post-supernova. We will discuss both the case of a single isolated star, and an isolated binary star system. It is pertinent to note that our binaries will be assumed to have formed together at birth -- thus having the same age -- rather than by, for example, the dynamical capture of a runaway star in a globular cluster, which introduces extra effects and considerations.

In this chapter, unless context otherwise requires, we use the following conventions surrounding the standard model and particle physics:

\begin{itemize}
    \item $\gamma$ refers to a photon,
    \item e$^+$ refers to a positron, and e$^-$ to an electron,
    \item $\nu$ refers to a neutrino, and $\nu_\text{e}$ to an electron neutrino specifically,
    \item ${}^\mathrm{A}_\mathrm{Z}\text{X}$ refers to atom X with atomic number Z and mass number A.
\end{itemize}

\section{Stellar Formation}\label{sec:stellar_formation}

\epigraph{Nature and nature's laws lay hid in night: God said, ``Let Newton be!'' and all was light.}{Alexander Pope}

From the point of view of stellar evolution, which starts in earnest at the \gls{zams}, the formation of stars is interesting but ultimately has little to no impact on the nuclear evolution of the star besides setting the initial mass. We provide a short recap of stellar formation here as scene-setting, but our stellar evolution models assume that a gas cloud has already collapsed and we have a star at its \gls{zams}.

Stars are born in clouds of gas and dust. Due to inhomegeneities in the dust formation, some regions become more dense than others. When a region of this gas with adiabatic constant $\gamma$ and mean molecular weight $\mu$ reaches the so-called Jeans Mass,

\begin{equation}
    M_\text{J} = \pqty{\frac{10k_\text{B}T}{3\pqty{\gamma-1}\mu m_\text{H}G}}^\frac32 \pqty{\frac{3}{4\pi\rho}}^\frac12 \sim \pqty{\frac{10k_\text{B}T}{G}R}^\frac32 \pqty{\frac{1}{M}}^\frac12,
\end{equation}

where the similarity is taking $\gamma=\frac53, \mu=\frac12$ for an adiabatic gas composed of H(II), then gravity becomes the dominant force in the clumps of gas. This causes a protostar to form. Generally speaking, this protostar slowly gains more mass from its surroundings \citep{Mckee:2007} and, as it does so, increases in temperature. When the core temperature reaches $\SI[parse-numbers=false]{10^7}{\kelvin}$, the \gls{pp_chain} begins. Bare protons are combined to form deuterium, positrons, and neutrinos. The positrons annihilate with electrons, producing photons, and the deuterium fuses with another proton to produce helium-3:

\begin{align*}
    \ce{^1H + ^1H &-> ^2H + e^+ + \nu_e} \\
    \ce{e^+ + e^- &-> \gamma} \\
    \ce{^1H + ^2H &-> ^3He + \gamma}.
\end{align*}

The three constituent parts of the \gls{pp_chain} are the \gls{pp1}, the \gls{pp2}, and the \gls{pp3}. Which path the star takes is dependent on the temperature:

\begin{itemize}
    \item The \gls{pp1} dominates at low temperatures, and is given by the reaction

    \begin{equation*}
        \ce{^3He + ^3He -> ^4He + 2^1H}.
    \end{equation*}

    \item The \gls{pp2} dominates at temperatures $>\SI{1.4e7}{\kelvin}$, and is given by the reaction chain

    \begin{align*}
        \ce{^3He + ^4He &-> ^7Be + \gamma} \\
        \ce{^7Be + e^-  &-> ^7Li + \nu_e} \\
        \ce{^7Li + ^1H  &-> 2^4He}. \\
    \end{align*}

    \item The \gls{pp3} dominates at temperatures $>\SI{2.3e7}{\kelvin}$, and is given by the reaction chain

    \begin{align*}
        \ce{^7Be + ^1H &-> ^8B + \gamma} \\
        \ce{^8B        &-> ^8Be^* + e^+ + \nu_e} \\
        \ce{^8Be^*     &-> 2^4He}, \\
    \end{align*}

    where a superscript asterisk denotes an unstable excited state.
\end{itemize}

All three paths in the \gls{pp_chain} ultimately produce $^4$He, with the key output difference being the total energy per $^4$He atom; the energy yield is highest for the \gls{pp1}, and smallest for the \gls{pp3}.

The burning of $^2$H -- deuterium -- occurs for a short fraction of the lifetime of the star, before core hydrogen burning begins in earnest. This energy release will sustain the star throughout its lifetime, providing outwards pressure resisting gravity pulling the star inwards, in a balance termed \textit{hydrostatic equilibrium}. Once hydrogen fusion has begun, we say that the star has reached \gls{zams}, and is now on the \gls{ms}, where it will spend approximately 90\% of its lifetime.

\section{Stellar Evolution}

\mediaepigraph{It did not last: the Devil howling ``Ho! Let Einstein be!'' restored the status quo.}{Answer to Pope's Epitaph for Sir Isaac Newton}{J. C. Squire}

Now that our protostar has evolved onto the \gls{ms}, it has become a star in its own right. Over the next few million years, it will continue to burn hydrogen into helium, forming a helium core. It is at this point that the evolution encounters a number of bifurcation points, depending on the mass and chemical composition of the star. In general, while the star lives, the four equations of stellar structure hold,

\begin{equation}
    \left.\begin{array}{lll}
        \dv{m}{r} &= 4\pi r^2\rho & \text{(mass continuity)}, \\
        \dv{p}{r} &= -\frac{GM\rho}{r^2} & \text{(hydrostatic equilibrium)},\\
        \dv{T}{r} &= -\frac{3\kappa\rho L}{64\pi r^2\sigma T^3} & \text{(energy transport), and} \\
        \dv{L}{r} &= 4\pi r^2\rho\epsilon & \text{(thermal equilibrium)},
    \end{array}\right\}
    \label{eq:stellar_structure}
\end{equation}

where $\epsilon$ is the rate of energy energation per unit mass, $\kappa$ is the opacity of the material, $\sigma$ is the Stefann-Boltzmann constant, $m$ is the mass internal to radius $r$, $p$ is pressure, $\rho$ is density, $M$ is total mass of the star, and $L$ is the luminosity.

In addition to \Cref{eq:stellar_structure}, an \gls{eos} demanding $\rho=\rho(P, T)$ is desired. We take our \gls{eos} from \citet{Pols:1995}, which built upon that in \citet{Eggleton:1973}. Critically, Pols' \gls{eos} includes the dissociation of molecular hydrogen, Coulomb interactions, and pressure ionization, which \citet{Eggleton:1973} lacked. The \gls{eos} is an approximation to Fermi-Dirac statistics, and originates from the free energy

\begin{equation}
    F(V, T, N_\text{e}, N_\text{e}) = -\frac13 aT^4 V + kT\sum_i N_\text{i} \pqty{\ln\frac{N_\text{i} h^3}{\omega_i V\pqty{2\pi m_i kT}^\frac32}-1+\frac{\chi_i}{kT}}+F_\text{e}(N_\text{e}, V, T)+\Delta F_\text{e}(N_\text{e}, V, T).
\end{equation}

These equations represent conservation of mass, hydrostatic equilibrium, energy transport, and thermal equilibrium, respectively. Knowing constants $\epsilon, \kappa$, we have four equations with four unknowns. It is usually convenient to use the \textit{Rosseland mean} opacity \citep{Rosseland:1924},

\begin{equation}
    \frac{1}{\kappa} = \frac{\pi}{acT^3}\int_0^\infty \frac{1}{\kappa_\nu}\pdv{B}{T}~\mathrm{d}\nu,
\end{equation}

where $a$ is the radiation density constant ($a=\SI{7.57e-15}{\erg\per\cubic\cm\per\quartic\kelvin}$) and $B$ is the black-body intensity at frequency $\nu$ and temperature $T$. The function $\kappa_\nu$ is obtainable from atom-electron-photon interaction \citep{Eldridge:2004a}: bound-bound, bound-free, free-free (bremsstrahlung) transitions, and electron scattering. $\kappa_\nu$ is thus related to the scattering cross-section $\sigma_\nu = \int_0^{2\pi}\int_0^\pi \dv{\sigma}{\Omega}\sin\theta\, \mathrm{d}{\theta}\, \mathrm{d}{\phi}$. The Rosseland mean becomes

\begin{equation}
    \kappa = \frac{T^5 a c^3 k_\text{B}}{\pi h^2 \int_0^\infty \frac{\nu^4}{\left(\cosh{\left(\frac{h \nu}{T k_\text{B}} \right)} - 1\right) \kappa_\nu}\, d\nu},
\end{equation}

though the integral cannot be analytically solved further. On the other hand, $\epsilon$ is obtainable from knowing the nuclear reactions occurring in the star's core, namely

\begin{equation}
    \epsilon = \sum_{i}^N \text{energy output of reaction } i.
\end{equation}

The reaction rates and energy outputs are typically inferred from empirical data; we take the interpolation in temperature from \citet{Caughlan:1988}, and correct the rates to approximate the cross-sections of \citet{Bahcall:1992, Bahcall:1995}.

All told, in the simplest sense, the internal \textit{structure} of the star can be modelled by hand by solving \Cref{eq:stellar_structure} from $m=0$ to $m=M$.

\subsection{The chemical composition of stars}

\alice

A star contains at its core a complex, multi-layered, nuclear furnace. As one atom approaches another, it must overcome the natural electrostatic repulsion so that the strong nuclear force ensures fusion occurs. This requires the bombarding atom to tunnel through the Coulomb barrier, instigating fusion.

We have already discussed the \gls{pp_chain}, fusing hydrogen into helium and up to beryllium. The two remaining parts of the story are the triple-$\alpha$ process and the CNO cycle. The triple-$\alpha$ process generates carbon, and is given by the reactions \citep{Carroll:2017}\footnote{The reversible reaction is due to Beryllium-8 being unstable; its half-life is on the order of $\SI{1e-16}{\second}$, and it decays back to 2$^4_2$He if it does not react further.}

\begin{align*}
    \ce{^4_2He + ^4_2He &\rightleftharpoons ^8_4Be}, \\
    \ce{^8_4Be + ^4_2He &-> ^12_6C + 2\gamma}. \\
\end{align*}

The CNO cycle generates elements up to fluorine via a branched cyclic reaction. The reactions are presented here, and are summarised in a flow chart in \Cref{fig:cno_cycle_schematic}. The start of the cycle, pre-branch, is given by

\begin{align*}
    \ce{^1H + ^{12}C &-> ^{13}N + \gamma} \\
    \ce{^{13}N       &-> ^{13}C + e^+ + \nu_e} \\
    \ce{^1H + ^{13}C &-> ^{14}N + \gamma} \\
    \ce{^1H + ^{14}N &-> ^{15}O + \gamma} \\
    \ce{      ^{15}O &-> ^{15}N + e^+ + \nu_e} \\
\end{align*}

Here, the reaction branches when $^{15}$N undergoes a proton capture. In 1 in every 2500 captures, the reaction branches to produce (at its heaviest) fluorine, in the \textit{NO cycle}:

\begin{align*}
    \ce{^1H + ^{15}N &-> ^{16}O + \gamma} \\
    \ce{^1H + ^{16}O &-> ^{17}F + \gamma} \\
    \ce{      ^{17}F &-> ^{17}O + e^+ + \nu_e} \\
    \ce{^1H + ^{17}O &-> ^{14}N + ^4He}. \\
\end{align*}

In the remaining cases, the reaction instead produces carbon-12, in the conclusion of the \textit{CN cycle}:

\begin{equation*}
    \ce{^1H + ^{15}N -> ^{12}C + ^4He + \gamma}.
\end{equation*}

\begin{figure}[!htbp]
    \centering
    \begin{tikzpicture}[xscale=2, yscale=1.5]
        \begin{tikz}
            \node (dummy) at (0,0) {};

            \draw[dashed, magenta] (1.5, 5.5) rectangle (3.5, 1.5);
            \node[magenta] at (2.5, 5.3) {\gls{pp_chain}};

            \draw[dashed, cyan] (-0.5, 4.5) rectangle (1.5, 1.5);
            \node[cyan] at (0.5, 4.3) {\gls{pp1}};

            \draw[dashed, red] (-3.5, 3.5) rectangle (-0.5, 1.5);
            \node[red] at (-2, 3.3) {Triple-$\alpha$ process};

            \draw[dashed, blue] (-2.5, 1.5) rectangle (4, -9.5);
            \node[blue] at (0.5, 1.3) {CNO cycle};

            % pp-chain

            \node (H1-1) at (3, 3) {$^1$H};
            \node (H1-2) at (3, 2) {$^1$H};
            \node (H2) at (2, 3) {$^2$H};
            \node (H1-3) at (2, 2) {$^1$H};
            \node (He3) at (0.5, 3) {$^3$He};

            \node (upper-H1-1) at (3, 4) {$^1$H};
            \node (upper-H1-2) at (3, 5) {$^1$H};
            \node (upper-H2) at (2, 4) {$^2$H};
            \node (upper-H1-3) at (2, 5) {$^1$H};
            \node (upper-He3) at (0.5, 4) {$^3$He};

            \draw[dashed, ->] (H1-2) -- (H1-1);
            \draw[dashed, ->] (H1-3) -- (H2);
            \draw[->] (H1-1) -- (H2);
            \draw[->] (H2) -- (He3);

            \draw[dashed, ->] (upper-H1-2) -- (upper-H1-1);
            \draw[dashed, ->] (upper-H1-3) -- (upper-H2);
            \draw[->] (upper-H1-1) -- (upper-H2);
            \draw[->] (upper-H2) -- (upper-He3);

            \node (H1-byproduct1) at (1, 2) {$^1$H};
            \node (H1-byproduct2) at (0, 2) {$^1$H};

            \draw[->] (upper-He3) -- (He3);
            \draw[->] (He3) -- (H1-byproduct1);
            \draw[->] (He3) -- (H1-byproduct2);

            \node (He4-2) at (-2, 3) {$^{4}$He};

            \draw[->] (He3) -- (He4-2);

            % Triple-alpha process
            \node (He4-1) at (-3, 3) {$^{4}$He};

            \node (Be8) at (-2, 2) {$^{8}$Be$^*$};
            \node (He4-3) at (-3, 2) {$^{4}$He};

            \node (C12) at (0, 0) {$^{12}$C};
            \node (2photons) at (-1, 2) {$2\gamma$};

            \draw[->] (He4-1) -- (He4-2);
            \draw[->] (He4-2) -- (Be8);

            \draw[->] (He4-3) -- (Be8);
            \draw[->] (Be8) -- (C12);
            \draw[->] (Be8) -- (2photons);

            \node (dots1) at (-3, 4) {$\dots$}; % These get produced via their own pp-chains
            \node (dots2) at (-3, 1) {$\dots$};

            \draw[->] (dots1) -- (He4-1);
            \draw[->] (dots2) -- (He4-3);

            % CNO cycle
            \node (N13) at (0,-1) {$^{13}$N};
            \node (C13) at (0,-2) {$^{13}$C};
            \node (N14) at (0,-3) {$^{14}$N};
            \node (O15) at (0,-4) {$^{15}$O};
            \node (N15) at (0,-5) {$^{15}$N};
            \node (O16) at (0,-6) {$^{16}$O};
            \node (F17) at (0,-7) {$^{17}$F};
            \node (O17) at (0,-8) {$^{17}$O};

            \node (proton1) at (-1, 0) {$^1$H};
            \node (proton2) at (-1, -2) {$^1$H};
            \node (proton3) at (-1, -4) {$^1$H};
            \node (proton4) at (-1, -5) {$^1$H};
            \node (proton5) at (-1, -6) {$^1$H};
            \node (proton6) at (-1, -8) {$^1$H};

            \node (photon1) at (1, 0) {$\gamma$};
            \node (photon2) at (1,-2) {$\gamma$};
            \node (photon3) at (1,-3) {$\gamma$};
            \node (photon4) at (1,-4) {$\gamma$};
            \node (photon5) at (1,-6) {$\gamma$};
            \node (photon6) at (1,-7) {$\gamma$};

            \node (photon7) at (2, -6) {$\gamma$};
            \node (helium1) at (3, -5) {$^4$He};

            \node (helium2) at (1, -9) {$^4$He};

            \node (e+1) at  (1, -1 +0.5) {e$^+$};
            \node (nue1) at (1, -1 -0.5) {$\nu_\text{e}$};
            \node (e+2) at  (1, -5 +0.5) {e$^+$};
            \node (nue2) at (1, -5 -0.5) {$\nu_\text{e}$};
            \node (e+3) at  (1, -8 +0.5) {e$^+$};
            \node (nue3) at (1, -8 -0.5) {$\nu_\text{e}$};

            \draw[->] (C12) -- (N13);
            \draw[->,dotted] (N13) -- (C13);
            \draw[->] (C13) -- (N14);
            \draw[->] (N14) -- (O15);
            \draw[->,dotted] (O15) -- (N15);
            \draw[->] (N15) -- (O16) node[below,midway,sloped] {\tiny{1/2500}};
            \draw[->] (O16) -- (F17);
            \draw[->,dotted] (F17) -- (O17);

            \draw[->] (N15) -- (2, -5) node[above,midway,sloped] {\tiny{2499/2500}} -- (2, 1) -- (0, 1) -- (C12);

            \draw[->] (2, -5) -- (photon7);
            \draw[->] (2, -5) -- (helium1);

            \draw[->,dashed] (proton1) -- (C12);
            \draw[->,dashed] (proton2) -- (C13);
            \draw[->,dashed] (proton3) -- (N14);
            \draw[->,dashed] (proton4) -- (N15);
            \draw[->,dashed] (proton5) -- (O16);
            \draw[->,dashed] (proton6) -- (O17);

            \draw[->] (C12) -- (photon1);
            \draw[->] (C13) -- (photon2);
            \draw[->] (N14) -- (photon3);
            \draw[->] (O15) -- (photon4);
            \draw[->] (O16) -- (photon5);
            \draw[->] (F17) -- (photon6);

            \draw[->] (N13) -- (e+1);
            \draw[->] (N13) -- (nue1);
            \draw[->] (N15) -- (e+2);
            \draw[->] (N15) -- (nue2);
            \draw[->] (O17) -- (e+3);
            \draw[->] (O17) -- (nue3);

            \draw[->] (O17) -- (0, -9) --++ (-2, 0) -- (-2,-3) -- (N14);

            \draw[->] (0, -9) -- (helium2);

            % legend

            \filldraw[densely dashdotted, fill=black!5] (1.5,-9) rectangle (3.5, -6.5);

            \node at (2.5, -6.9) {\Large \textbf{Key}};

            \draw[->,dotted] (2, -8.5) -- (3, -8.5) node[above,midway,sloped] {Radioactive decay};
            \draw[->,dashed] (2, -8) -- (3, -8) node[above,midway,sloped] {Proton bombardment};
            \draw[->] (2, -7.5) -- (3, -7.5) node[above,midway,sloped] {Reaction product};
        \end{tikz}
    \end{tikzpicture}
    \caption[A schematic of the reactions leading up to the CNO cycle.]{A schematic of the reactions leading up to the CNO cycle. The \gls{pp1} chain is replaced by the \gls{pp2}/\gls{pp3} chain, depending on the temperature of the star. The \glspl{pp_chain} generating the $^4$He atoms on the leftmost side of the chain are omitted for simplicity. Once the $^{15}$N atom has been produced, the reaction cycle is essentially self-sustaining, provided there are protons available to bombard the reactants.}
    \label{fig:cno_cycle_schematic}
\end{figure}

Further reactions essentially proceed similarly, and the core can produce up to iron before the reaction becomes endothermic. However, these elements do not dominate the star, with the exception of forming a \gls{CO_core} and \gls{ONe_core} later in the evolution of the star. We thus group the elements into hydrogen, helium, and metals (which we define as anything with atomic number $\geq3$, i.e., lithium and heavier elements). It is convenient to speak of the mass fraction of the element, i.e., the fraction by mass of a star that is a certain element. For our purposes, this is expressible as

\begin{equation}
	X+Y+\sum_{i=3}^{118} \sum_{j} {}^{i}_{j}\text{X} = 1, \label{eq:mass_fracs}
\end{equation}

where $X$ is the mass fraction of hydrogen, $Y$ of helium, and ${}^{i}_{j}\text{X}$ as the metal\footnote{It is custom in astronomy to refer to anything with an atomic number greater than 2 as a metal, rather than the conventional definition of a material conductive at absolute zero, including most of the d-block, s-block, f-block, and about half of the p-block on the periodic table.} with atomic number $i$ and mass number $j$. When speaking about the chemical composition of a star it is also convenient to speak of the \textit{metallicity}, the fraction at \gls{zams} of the star that is metal. For this, we define the metallicity as

\begin{equation}
    Z \equiv \sum_{i=3}^{118} \sum_{j} {}^{i}_{j}\text{X},
\end{equation}

such that \Cref{eq:mass_fracs} becomes

\begin{equation}
    X+Y+Z = 1.
\end{equation}

Of note, abundances are not always in the literature referred to in this fashion. In some works, such as \citet{Asplund:2021}, abundances are reported in the form $\mathcal{X}_i=12+\log_{10}\left(\nicefrac{\text{X}_i}{\text{H}}\right)$. To convert between the two, the relationship

\begin{equation}
    \mathcal{X}_i = 12+\log_{10}\left(\frac{Z\times \frac{X_{i, \text{init}}}{56}}{0.75-2.5Z}\right)
\end{equation}

is employed, where $X_{i,\text{init}}$ is the initial mass fraction of a given element (which we take from \citealt{Grevesse:1993}, summarised in \Cref{tab:initial_abundances}).

\begin{table}[!htbp]
    \centering
    \begin{tabular}{cc|cc}
        \toprule
        Element & Initial abundance & Element & Initial abundance \\
        \midrule
        Hydrogen & 0.7   & Neon      & 0.099 \\
        Carbon   & 0.173 & Magnesium & 0.038 \\
        Nitrogen & 0.053 & Silicon   & 0.080 \\
        Oxygen   & 0.482 & Iron      & 0.072 \\
        \hline

        \hline
    \end{tabular}
    \caption[The unweighted initial abundances by mass of our models, unless otherwise stated.]{The unweighted initial abundances by mass of our models, unless otherwise stated. Metals are then weighted by metallicity, such that, for example, the carbon abundance is $0.173\times0.020=0.00346$. The balance is assumed to be in Helium.}
    \label{tab:initial_abundances}
\end{table}

\subsection{Evolutionary Pathways}

In this section, we examine the three major regimes of stellar evolution for single-stars: low-mass, intermediate-mass, and massive stars. Stars above $M_\text{ZAMS}\approx\SI{80}{\msun}$ are out of the scope of this thesis -- although they exist, and the \gls{bpass} \gls{imf} reaches \SI{300}{\msun} \citep{Eldridge:2017}, the systems of interest in future chapters are at most $\sim\SI{80}{\msun}$, and thus very massive stars are out of scope for this work.

\subsubsection{Low-Mass Stars: \texorpdfstring{$\SI{0.08}{\msun} < M < \SI{2}{\msun}$}{Stars with between 0.08 and 2 times the mass of the sun}}\label{sec:low_mass_stars}

Stars below $\approx\SI{0.5}{\msun}$ do not ignite helium, and become helium \glspl{wd}. Above this, they ignite helium degenerately in their cores, leading to thermal runaway. The star can peak at a luminosity close to $10^{11} L_\odot$ (approximately 100 times the peak luminosity of SN 1987A, per Figure 5 of \citet{Arnett:1989}). However, this peak in luminosity is attributable to nuclear fusion in the core, and the energy does not reach the surface, let alone escape into the \gls{ism} -- rather, it is absorbed into the outer layers of the star. During this extremely brief thermal runaway, the luminosity begins to settle and the star is able to continue evolving. Models of low mass stars fail at this stage and care is required to evolve them. This phenomena is called the \textit{helium flash} and is discussed further in \Cref{sec:numerical_instabilities}.

After the ignition of helium, these stars cross the so-called Hertzsprung Gap -- the region where very few stars are observed empirically. This region is bounded (on a colour-magnitude diagram, which is a transform of the \gls{hrd}) by the points $\pqty{M_v, B-V}=\left\{\pqty{2,0.5}, \pqty{-7, 0.1}, \pqty{-6, 1.6}\right\}$ \citep{Hoyle:1960}, where $M_v$ is the V-band magnitude (magnitude in the visual band), and $B-V$ is the colour index of the star. The phenomenon of a gap existing is attributable to the fact that stars cross this gap exceedingly quickly: whereas it spends $\sim90\%$ of its life on the \gls{ms}, it spends $\sim2\%$ of its life crossing the Hertzsprung gap \citep{Hoyle:1960}.

\subsubsection{Intermediate-Mass Stars: \texorpdfstring{$\SI{2}{\msun} < M < \SI{8}{\msun}$}{Stars with between 2 and 8 times the mass of the sun}}\label{sec:intermediate_mass_stars}

Intermediate mass stars exist above the threshold for the helium flash to occur. Helium ignition is not degenerate and the development of a helium core is not impeded. As the star ascends the \gls{agb}, the core becomes helium-exhausted and it becomes a red giant, with a radius many hundreds or even thousands of times greater than the Sun. Its mass, sustained prior to the \gls{agb} via nuclear fusion, begins to decrease. An example of intermediate-mass evolution is shown in \Cref{fig:intermediate_mass}.

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{images/TheorySection/intermediate_mass.pdf}
    \caption[The evolution of an \SI{4}{\msun} star from \acrshort{zams} until \acrshort{CO_core} development.]{The evolution of an \SI{4}{\msun} star from \acrshort{zams} until \acrshort{CO_core} development. The final core abundances are $Z_\text{C} \approx 0.44$ and $Z_\text{O}\approx0.53$.}
    \label{fig:intermediate_mass}
\end{figure}

\subsubsection{Massive Stars: \texorpdfstring{$\SI{8}{\msun} < M < \SI{80}{\msun}$}{Stars with between 8 and 80 times the mass of the sun}}\label{sec:massive_stars}

Massive stars are the key focal objects of this thesis, and in many ways offer some of the most exciting phenomena we can probe. In \Cref{fig:hr_diagram_massive_stars} we show the variety of evolution that this regime has, alongside the \gls{zams} track from \SI{8}{\msun} to \SI{90}{\msun}. The key features are as follows. When a massive star exhausts its supply of hydrogen in its core and evolves off the \gls{ms} to begin burning helium, it generally bypasses the Hertzsprung gap and evolves up the \gls{rgb}. From here, stars below $\approx\SI{15}{\msun}$ generally engage in a ``blue loop'', where is remains at a roughly constant radius whilst increasing in temperature, before cooling at a slightly higher luminosity. This is due to excess helium being burned in shells above the core \citep{Walmswell:2015}. We note that our models include blue loops, as shown in \Cref{fig:hr_diagram_massive_stars} and \Cref{fig:hr_diagram_8msun_star}.

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{images/TheorySection/10_msun_jumps_new.pdf}
    \caption[The evolution of stars in the range $0.5-80\unit{\msun}$.]{The evolution of stars in the range $0.5-80\unit{\msun}$. The dashed black line represents the \acrfull{zams}, which is the origin point of all of our models, and (in this plot) extends from \SI{0.5}{\msun} to \SI{300}{\msun}. The blue triangle is the (highly approximate) bounds of the Hertzsprung gap. Crosses indicate binaries in the Double Eclipsing Binaries catalogue \citep[DEBCAT;][]{Southworth:2015}, and illustrate the apparent ``zone of avoidance'' that defines the Hertzsprung gap.}
    \label{fig:hr_diagram_massive_stars}
\end{figure}

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{images/TheorySection/8_msun_star_annotated.pdf}
    \caption[The evolution of an \SI{8}{\msun} star.]{The evolution of an \SI{8}{\msun} star. Highlighted is the star's \acrfull{zams}, \acrfull{tams}, the blue loop, and the \acrfull{agb}. The Hertzsprung gap is also highlighted. In this particular model, numerical instabilities in the leftmost (hottest) region of the plot have been highlighted in pink. This is normally removed in postprocessing though has been left in as an illustration.}
    \label{fig:hr_diagram_8msun_star}
\end{figure}

\subsection{Stellar Atmospheres}

As a final note in the evolution of a single star, we must briefly address what occurs about the photosphere of the star. A star is not a planet -- there is no firm, opaque boundary demarcating the inner of a star from the outer. The common definition of the ``surface'' of a star is the surface of last scattering (equivalently, the photosphere), which is the point radially in which the mean free path $\ell_\gamma$ of a photon emitted from the core of the star becomes meaningfully long (internally, $\ell_\gamma\sim\SI{2}{\cm}$, externally, $\ell_\gamma\sim\SI{4.4}{\pc}$).\footnote{The mean free path is a function of the particle under consideration: for a neutrino, internally $\ell_\nu\sim\SI{32}{\pc}$ and externally $\ell_\nu\sim\SI{81}{\ronna\pc}\sim 3\times10^{18}$ times the diameter of the observable Universe.} However, outflows from the star as well as the potential interaction of a partner in a common-envelope event mean that this surface is not necessarily firm: there is no sharp turn from stellar material to empty space. Thus, the simulation of stellar atmospheres is of interest to the stellar astrophysicist.

However, this introduces another rather large layer of complexity. The synthesis of stellar spectra is not a trivial task, even without considering the complexity of evolving the star simultaneously. \citet{Groh:2014} is one of the very few instances of coupled spectroscopic calculations with evolutionary calculations, which only evolved a single star. Moreover, this thesis is concerned with detailed modelling of the stellar \textit{interior}, and thus the atmosphere is an interesting sidenote, but one we shall not pursue.

\section{Supernovae}

\twinEpigraphs{And on the pedestal these words appear:\\
``My name is Ozymandias, king of kings:\\
Look on my works, ye Mighty, and despair!''\\
Nothing beside remains.}{Percy Bysshe Shelley, \textit{Ozymandias}}{Pallida mors {\ae}quo pulsat pede pauperum\\
tabernas regumque turres.\\
Pale death knocks with the same tempo upon the\\
huts of the poor and the towers of kings.}{Horace}

A star's lifetime is not infinite. The key assumption underpinning stellar evolution is that of hydrostatic equilibrium: the counterplay of gravity against outwards pressure. In short, a dyanmically stable, non-rotating star composed of an ideal gas with adiabatic constant $\gamma$ and gravitational potential energy $\Omega$ radiates energy via

\begin{equation}
    L = -\dot{E} = -\frac{3\gamma-4}{3\pqty{\gamma-1}} \dv{\Omega}{t}-\dv{\gamma}{t}\pqty{\frac{1}{\pqty{\gamma-1}^2}},
\end{equation}

If the chemical gradient is largely unchanged, $\dv{\gamma}{t}\sim0$ so the luminosity reduces to

\begin{equation}
L = -\frac{3\gamma-4}{3\pqty{\gamma-1}} \dv{\Omega}{t}\quad\text{where}\quad U=-\frac{1}{3\pqty{\gamma-1}} \Omega,
\end{equation}

where $U$ is the total internal energy of the star. Since $\dot{E}<0$ for a star radiating energy, and $\Omega<0$ by definition (since assembling a star is energetically favourable), we have that $\dot{\Omega}<0$ and $\dot{U}>0$, i.e., that as the star radiates energy it attempts to shrink. This shrinking is arrested by the nuclear fusion in the core providing outwards pressure, meaning the star is stable and resists explosion. As the star continues to burn through its fuel supply and eventually exhaust it, gravity will win as the dominant force, and result in the star imploding in a \gls{ccsne}. From a thermodynamic perspective, as the star evolves, it shrinks as energy is bled off into the interstellar medium. This shrinkage compresses and heats the core, meaning the star's specific heat capacity is negative. This system is thermodynamically unstable, and thus every star either collapses into a remnant (cf. \gls{bh}, \gls{ns}) or is sustained in some form by electron degeneracy pressure (cf. \gls{wd}).

In any case, the breaking of hydrostatic equilibrium results generally results in\footnote{The extent of the veracity of this statement is an area of active research: \citet{Reynolds:2015} presents a short summary.} a supernova, which can be classified into subtypes, as shown in \Cref{fig:supernova_taxonomy}. In this event, gravity pulls the stellar core inwards, before ``bouncing'' and ejecting the envelope into the interstellar medium. The physics of this approximately second-long event is highly uncertain, and is in general unable to be modelled by detailed stellar evolution codes. All models we generate -- assuming they have not been terminated earlier -- will terminate at the moment of supernova.

\begin{figure}[!htbp]
    \centering
    \begin{tikzpicture}
        \usetikzlibrary{arrows.meta,arrows,positioning}
        \newcommand{\XScale}{1.65}
        \newcommand{\YScale}{3}

        \tikzstyle{decisionpoint} = [draw,rectangle,text centered]
        \tikzstyle{leaf} = [text width=5cm,text centered,font=\bfseries]
        \tikzstyle{yes} = [color=black!30!green,font=\bfseries]
        \tikzstyle{no} = [color=red!75,font=\bfseries]
        \tikzstyle{ambig} = [color=black!30!orange,font=\bfseries]

        \node[text width=4cm,align=center,decisionpoint] (hydrogen) at (1*\XScale,0*\YScale) {Hydrogen present?};
        \node[text width=4cm,align=center,decisionpoint,fill=black!20] (supernova) at (-2*\XScale,0*\YScale) {Supernova\\occurs};

        \node[text width=4cm,align=center,decisionpoint] (typeI) at (-1*\XScale,-1*\YScale) {Silicon present?};
        \node[text width=4cm,align=center,leaf] (typeIa) at (-2*\XScale,-2*\YScale) {Type Ia};
        \node[text width=4cm,align=center,decisionpoint] (typeIbc) at (0*\XScale,-2*\YScale) {Helium present?};
        \node[text width=4cm,align=center,leaf] (typeIb) at (-1*\XScale,-3*\YScale) {Type Ib};
        \node[text width=4cm,align=center,leaf] (typeIc) at (1*\XScale,-3*\YScale) {Type Ic};

        \node[text width=4cm,align=center,decisionpoint] (typeII) at (3*\XScale,-1*\YScale) {Does the hydrogen line\\stay there over time?};
        \node[text width=4cm,align=center,leaf] (typeIIb) at (2*\XScale,-2*\YScale) {Type IIb};
        \node[text width=4cm,align=center,decisionpoint] (typeIIpln) at (4*\XScale,-2*\YScale) {Are the hydrogen lines narrow?};
        \node[text width=4cm,align=center,leaf] (typeIIn) at (3*\XScale,-3*\YScale) {Type IIn};
        \node[text width=4cm,align=center,decisionpoint] (typeIIpl) at (5*\XScale,-3*\YScale) {Does the light curve plateau, or decrease linearly?};
        \node[text width=4cm,align=center,leaf] (typeIIp) at (4*\XScale,-4*\YScale) {Type II-P};
        \node[text width=4cm,align=center,leaf] (typeIIl) at (6*\XScale,-4*\YScale) {Type II-L};

        \draw[-{Stealth[length=3mm, width=2mm]}] (hydrogen) -> (typeI) node[above,sloped,midway,no] {No};
        \draw[-{Stealth[length=3mm, width=2mm]}] (typeI) -> (typeIa) node[above,sloped,midway,yes] {Yes};
        \draw[-{Stealth[length=3mm, width=2mm]}] (typeI) -> (typeIbc) node[above,sloped,midway,no] {No};
        \draw[-{Stealth[length=3mm, width=2mm]}] (typeIbc) -> (typeIb) node[above,sloped,midway,yes] {Yes};
        \draw[-{Stealth[length=3mm, width=2mm]}] (typeIbc) -> (typeIc) node[above,sloped,midway,no] {No};

        \draw[-{Stealth[length=3mm, width=2mm]}] (hydrogen) -> (typeII) node[above,sloped,midway,yes] {Yes};
        \draw[-{Stealth[length=3mm, width=2mm]}] (typeII) -> (typeIIb) node[above,sloped,midway,no] {No};
        \draw[-{Stealth[length=3mm, width=2mm]}] (typeII) -> (typeIIpln) node[above,sloped,midway,yes] {Yes};
        \draw[-{Stealth[length=3mm, width=2mm]}] (typeIIpln) -> (typeIIn) node[above,sloped,midway,yes] {Yes};
        \draw[-{Stealth[length=3mm, width=2mm]}] (typeIIpln) -> (typeIIpl) node[above,sloped,midway,no] {No};
        \draw[-{Stealth[length=3mm, width=2mm]}] (typeIIpl) -> (typeIIp) node[above,sloped,midway,ambig] {Plateau};
        \draw[-{Stealth[length=3mm, width=2mm]}] (typeIIpl) -> (typeIIl) node[above,sloped,midway,ambig] {Decrease};

        \draw[-{Stealth[length=3mm, width=2mm]}] (supernova) -> (hydrogen);

    \end{tikzpicture}
    \caption[A rough outline of supernova classification.]{A rough outline of supernova classification. One can see that the presence of hydrogen lines distinguishes a Type I from Type II supernova. Type II supernova subtypes are determined by the behaviour of their light curves -- how the apparent magnitude changes against time -- and thus are determined by the luminosity of the supernova, something detailed models are not able to model. On the other hand, the presence of hydrogen/silicon/helium in the supernova is something that detailed codes \textit{are} able to model. There are further delineations into Type Ia/b/c-pec and Type IIpec, denoting peculiar supernovae, however these are rare and are in some sense exception categories, where the behaviour does not fit nicely into a taxonomy tree.}
    \label{fig:supernova_taxonomy}
\end{figure}

Importantly for the nucleosynthesis of iron-group and heavier elements (which cannot be formed via hydrostatic nuclear fusion,\footnote{Strictly speaking, fusion can occur for elements heavier than Iron-56, but the reaction is endothermic; it requires more energy than is released, meaning it is unsustainable.} as shown in \Cref{fig:BEPN}), the supernova shockwave propagating through the core causes a new phase of nucleosynthesis, albeit briefly.

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{images/TheorySection/BEPN_HalfLife.pdf}
    \caption[The binding energy per nucleon and half-lives for elements up to Oganesson.]{\textit{Top}: The binding energy per nucleon and half-lives for elements up to Oganesson. The most stable element of fusion here is Nickel-62. Elements lighter than Nickel-62 will fuse towards it, and elements heavier will undergo fission to reach it. \textit{Bottom:} The half-life in seconds across the same isotopes. Marked on the colorbar axis are \SI{1}{\year}, \SI{1000}{\year}, and $t_\text{H}=\SI{13.8}{\giga\year}$. Stable elements are marked in brown. Underlying data for both plots are from the International Atomic Energy Agency.}
    \label{fig:BEPN}
\end{figure}

\subsection{Type Ia supernovae}

There is another class of supernovae we consider in this thesis, namely type Ia supernovae. These are formed when a \gls{wd} star exceeds the maximum mass by which electron degeneracy pressure can sustain the star. This mass is termed the Chandrasekhar mass, $M_\text{ch}\approx\SI{1.44}{\msun}$. When material accretes onto a white dwarf, its mass will increase, and if the accretion does not stop, the mass will exceed the Chandrasekhar mass. This means electron degeneracy pressure is no longer able to sustain the star, and the core collapses further into a Type Ia (or thermonuclear) supernova.

\section{Two to tango: Binary Evolution}\label{sec:binary_evolution}

Introducing a second star into the mix complicates the evolution. Whereas before a star losing mass (through, for example, stellar winds) was assumed to eject it into the \gls{ism}, it may now accrete that material directly onto its companion. In addition, either star may in principle increase in radius enough to overlap with its companion, or even touch surfaces -- introducing new dynamics into the mix.

The most marked change for a binary compared to a single star is that its trajectory on the \gls{hrd} is altered. The \gls{ms} is changed somewhat, but (for longer than $\sim$\SI{5}{\day} periods) the interaction takes place post-\gls{ms} and is shown in \Cref{fig:hrd_binary_effects}.

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{images/TheorySection/interaction_effect.pdf}
    \caption[The \acrshort{hrd} for an \SI{21}{\msun} star with an (unshown) \SI{10}{\msun} companion with varying period.]{The \acrshort{hrd} for an \SI{21}{\msun} star with an (unshown) \SI{10}{\msun} companion with varying period. In this figure, only the initial binary period was free to change between runs. Short periods interact closer to the \acrshort{ms}. The evolution was terminated 1,000 models after the primary star achieved \acrshort{rlof} for the first time.}
    \label{fig:hrd_binary_effects}
\end{figure}

\subsection{Binary star kinematics}\label{sec:binary_kinematics}

There are several formation channels for a binary star system. Binaries may be formed from birth (as discussed in \Cref{sec:stellar_formation}, with the caveat that in this scenario two clumps of gas form near each other), via fragmentation of a protostar's disc, dynamical capture of one star by another (comparable to one of the suggested origins of Phobos and Deimos, per \citet{Burns:1978}), or via a higher multiplicity system ejecting one or more of its constituent stars, perhaps via a natal supernova kick.

In any case, once formed and the orbit is stabilised, binary systems obey Kepler's three laws. In the generalised binary regime, however, the assumption that $M_1\gg M_2$ (which underpins most statements of Kepler's laws) is not necessarily true -- for instance, the system LSS 3074 has two stellar components with \gls{zams} masses of approximately \SI{30}{\msun} and \SI{10}{\msun} \citep{Raucq:2017}. Moreover, the fact that there are two stars in orbit rather that one object orbiting another means that Kepler's laws must be stated more precisely. Formally, they can be expressed as (see \Cref{chap:additional_derivations} for derivations):

\begin{enumerate}
    \item All orbits are ellipses, with both stars orbiting a common barycentre at the focus of the ellipse,
    \item A line segment joining the two stars sweeps out equal areas in equal times,
    \item The square of the period of the system is proportional to the cube of the semimajor axis of the ellipse (the binary separation), with the proportionality term dependent on the sum of the masses.
\end{enumerate}

The third law can be stated more precisely as

\begin{equation}
    T^2 = \frac{4\pi^2}{G(M_1+M_2)}a^3,
\end{equation}

where $T$ is the period of the binary, $M_1$ and $M_2$ the constituent masses, $G=\SI{6.67e-11}{\meter\cubed\per\kilogram\per\second\squared}$ is the universal gravitational constant, and $a$ is the semimajor axis of the binary. A schematic of the orbit of a binary system is given in \Cref{fig:bss_schematic}.

\begin{figure}[!htbp]
    \centering
    \input{chapters/figures/binary_schematic}
    \caption[A schematic of a simple binary star system.]{A schematic of a simple binary star system with eccentricity $e$, semimajor axis $a$, and semiminor axis $b$. The centre of mass (barycentre) is shown as a blue dot. Here, $M_1$ is the heavier star, which orbits at a smaller distance $r_1$ to the barycentre by Kepler's first and Newton's second laws. The elliptical orbital trajectory is shown in grey. Figure adapted from \citet{Richards:2021}.}
    \label{fig:bss_schematic}
\end{figure}

\subsection{Roche Lobes}\label{sec:roche_lobes}

A binary system is complicated by the presence of its companions in several different ways. As alluded to in the previous section, the presence of a companion warps space-time in such a way that material can be quite distorted from a sphere or oblate spheroid but still remain gravitationally bound to the system. In the extremal case, a star can grow to fill or even exceed its Roche Lobe, which is the tear-drop shaped equipotential surface within which material remains gravitationally bound to its star. In \Cref{fig:roche_potentials} we display these equipotential surfaces for a range of different mass ratios. It is pertinent to note that the shape of all of the Roche Lobes change rapidly at low mass ratios, however at higher mass ratios the shapes change less rapidly. In an equal-mass ratio binary, the binary configuration has rotational symmetry about the $\mathcal{L}_1$ point, and reflectional symmetry about the $x$-axis and the line $y=\mathcal{L}_1$.

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.85\textwidth]{images/TheorySection/roche_potentials.pdf}
    \caption[The dimensionless Roche potential as a function of mass ratio $q$.]{The dimensionless Roche potential as a function of mass ratio $q$. Note that the shape of the Roche potential changes markedly at low mass ratio, and changes more slowly as the mass ratio approaches unity. Distances have been normalised such that the primary star is at $(x,y,z)=(0,0,0)$ and the secondary star is at $(x,y,z)=(1,0,0)$, i.e., we measure distances in units of separation and that the coordinate system is centered on the primary star.}
    \label{fig:roche_potentials}
\end{figure}

There are two primary Roche Lobe points we consider: the $\mathcal{L}_1$ point and the $\mathcal{L}_2$ point. The $\mathcal{L}_1$ point (also known as the inner Lagrangian point) exists on the line connecting the centres of the two stars, the $\mathcal{L}_2$ point (the outer Lagrangian point) is located on the other side of the secondary to the inner point. The dimensionless $x$-ordinate of the $\mathcal{L}_1$ point, $\xi$, measured in terms of the separation $a$, is given as the solution to

\begin{equation}
    0=\xi-\frac{\xi_2}{\pqty{\xi-\xi_1}^2}-\frac{\xi_1}{\pqty{\xi-\xi_2}^2},\label{eq:roche_lobe_condition}
\end{equation}

where $\xi_1=\frac{M_2}{M_1+M_2}$, and $\xi_2=\frac{M_1}{M_1+M_2}$.

This is a quintic expression in $\xi$, and solving this results in the real $\xi$-ordinates of five extrema. The extrema are labelled $\mathcal{L}_1$ through $\mathcal{L}_5$. $\mathcal{L}_1, \mathcal{L}_2, \mathcal{L}_3$ are saddle points of the potential, and $\mathcal{L}_4$ and $\mathcal{L}_5$ are maxima. No analytical function for $\xi$ exists unless either or both of $\xi_1, \xi_2$ are zero (which would only be the case for a single star, and thus the Roche lobe would not exist in the first instance), thus we must make approximations.

\subsubsection{The \texorpdfstring{$\mathcal{L}_1$}{inner Lagrangian} point}

The inner Lagrangian point has the approximation from \citet{Eggleton:1983}, which is

\begin{equation}
    \frac{R_\text{L}}{a} \approx \frac{0.49q^\frac23}{0.6q^\frac23+\ln\pqty{1+q^\frac13}}.\label{eq:RL1_eggleton}
\end{equation}

We show the circle with this radius $R_\text{L}$ in \Cref{fig:eggleton_approx_schematic} plotted against the exact solution for $\eta=\mathcal{L}_1$. Eggleton's formula is a volume-equivalent one; although his radius $R_\text{L} < \xi$, the volume of an Eggleton-Roche sphere $\frac43 \pi R_\text{L}^3$ is approximately equal (to within 1 per cent) of the numerically integrated volume, as is shown in \Cref{fig:eggleton_approx}.

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{images/TheorySection/RLs_Eggleton_Formula.pdf}
    \caption[A schematic of the Roche lobe formula \Cref{eq:RL1_eggleton}.]{A schematic of the Roche lobe formula \Cref{eq:RL1_eggleton}. Here, the exact solution to the Roche lobe formula $\xi$ in \Cref{eq:roche_lobe_condition} (orange) vs the fit provided by \citet{Eggleton:1983} (blue). Although precise details of the Roche lobe, such as the apex of the teardrop at the point $\xi$, are lost, the volume is preserved to within 1 per cent accuracy over all mass ratios.}
    \label{fig:eggleton_approx_schematic}
\end{figure}

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{images/TheorySection/eggleton_justification.pdf}
    \caption[The volume of the Roche Lobe computed numerically through Monte-Carlo integration vs the volume assuming \Cref{eq:RL1_eggleton}.]{The volume of the Roche Lobe computed numerically through Monte-Carlo integration (orange) vs the volume assuming \Cref{eq:RL1_eggleton} (blue). The Eggleton approximation fits well to the volume, with around 1 per cent error across the entire mass ratio range (truncated here to $q\leq 1$).}
    \label{fig:eggleton_approx}
\end{figure}


\subsubsection{The \texorpdfstring{$\mathcal{L}_2$}{outer Lagrangian} point}

The outer Lagrangian point, $\mathcal{L}_2$, is given by a different zero of the potential function. The approximation we use is that of \citet{Marchant:2016}, where

\begin{equation}
    R_\text{L2} = R_\text{L}(q) \pqty{1+\frac{0.299\arctan\pqty{{1.84q^{-0.397}}}}{q^{0.520}}},\label{eq:RL2_marchant}
\end{equation}

and $R_\text{L}(q)$ is given by \Cref{eq:RL1_eggleton}.

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{images/TheorySection/marchant_justification.pdf}
    \caption[As for \Cref{fig:eggleton_approx}, but for the $\mathcal{L}_2$ point.]{As for \Cref{fig:eggleton_approx}, but for the $\mathcal{L}_2$ point with formula given by \Cref{eq:RL2_marchant} \citep{Marchant:2016}.}
    \label{fig:marchant_approx}
\end{figure}

We note that \citet{Marchant:2016} uses an inverse mass ratio definition compared to \citet{Eggleton:1983}, that is, \citet{Marchant:2016} takes $q_\text{Marchant}=\frac{M_1}{M_2}$ whereas \citet{Eggleton:1983} takes $q_\text{Eggleton}=\frac{M_2}{M_1}=\frac{1}{q_\text{Marchant}}$. This is accounted for in \Cref{eq:RL2_marchant} such that the mass ratios are consistent (and we take $q=q_\text{Eggleton}$), but other texts do not necessarily differentiate or describe the difference.

\subsection{Mass Transfer}\label{sec:mass_transfer}

\alice

When two stars are sufficiently close in proximity, surface and near-surface events can cause accretion of material from one star to another due to the primary's stellar winds (whereas in the single-star case, this would be matter lost to the \gls{ism}). The nature of mass transfer is a complicated affair; most binary star population synthesis codes typically inherit their mass loss prescriptions from papers such as \citet{Bondi:1944, Hurley:2002}, and this approach is what we detail in this section.

Mass transfer can broadly be sectioned into four main categories:

\begin{enumerate}
    \item $\dot{M}_\text{wind}$, where matter is lost through stellar winds,
    \item $\dot{M}_\text{RLOF}$, where matter is transferred through one (or more) of a the Lagrange points in a binary (cf. \Cref{sec:roche_lobes}),
    \item $\dot{M}_\text{acc}$, where matter is transferred through accretion (or interaction with a disk), and
    \item $\dot{M}_\text{Bondi}$, where matter proceeds through a \citet{Bondi:1944} type mechanism.
\end{enumerate}

Our mass loss rate for each star is constructed from these four terms as

\begin{equation}
    \dot{M} = \dot{M}_{\text{wind}} - \dot{M}_{\text{Bondi}} + \dot{M}_{\text{RLOF}} - \dot{M}_{\text{acc}}.\label{eq:mass_loss_full}
\end{equation}

\subsubsection{Wind mass-loss}

Wind mass loss computations are (generally) performed using a mix of theoretical and empiricial mass-loss rate prescriptions of \cite{De_jager:1988} and \citet{Vink:2001}. The precise scheme is detailed in \citet{Eldridge:2017}.\footnote{In other codes such as \codename{MESA}, this is referred to as the ``Dutch'' mass loss prescription owing to the nationality of the authors of \citet{De_jager:1988, Nugis:2000, Vink:2001}.} There are two critical modifications we make to their scheme.

The first modification surrounds the transition into the \gls{wr} regime. Traditionally this has been a sharp transition when the surface hydrogen mass fraction becomes less than 0.4 and the surface temperature is above \SI[print-unity-mantissa=false]{1e4}{\kelvin}. This is a reasonable approximation, though if a star reaches this phase and is \textit{not} yet a \gls{wr} star, the code will fail. To account for this we gradually turn on these enhanced \gls{wr} rates when this condition approaches, smoothing the transition.

The second modification we have made is that if a star has a hydrogen exhausted core and begins approaching the \gls{wr} temperature range, we assume that \gls{wr} mass loss must begin and thus turn on the slowly-turning on \gls{wr} regime.

Mathematically, the computation for $\dot{M}_\text{wind}$ is more involved, though follows the same essential structure as \citet{Vink:2001}. Details are provided in \Cref{chap:mass_loss_construction}.

\subsubsection{\acrfull{rlof}}\label{sec:_acrfull_rlof}

Matter extruded from the Lagrangian points is assumed to transfer with a mass loss rate similar to \citet{Hurley:2002}. We take

\begin{equation}
        \dot{M}_{\text{RLOF},i} = k \cdot\max\left(\ln\left(\frac{R_{i}}{R_{\text{RL},i}}\right), 0\right)^3 \left(\frac{M_{i}}{M_\odot}\right)^2~\si{\msun\per\year}.\label{eq:accretion_ML_rate}
\end{equation}

Here, $k$ (referred to in our code as RMT) parameterises effectively how much of the star we permit to exist outside of its Roche lobe before it is accreted -- i.e., the voracity of \gls{rlof}. The choice of this can be somewhat arbitrary. \citet{Hurley:2002} for example took $k=\num{3e-6}\min\left\{M_1, 5\right\}^2$, where the coefficient was ``chosen by experiment to ensure that the mass transfer is steady'', and warned that for degenerate stars a more appropriate choice would be to increase this by a factor of $10^3$, corroborated by \citet{Claeys:2014}. We choose to adopt $k=\num{3e-6}\times10^3=\num{3e-3}$, with no dependence on the mass. This permits the star to overfill its Roche lobe even in tight binaries, but still allows efficient mass transfer.

Matter exceeding $\mathcal{L}_2$ (the outer Lagrange point) does not currently receive special treatment. The implications of $\mathcal{L}_2$ overflow are significant. This ejection will lead to unstable mass transfer, shrinking the orbit and likely inducing a \gls{cee} event \citep{Fragos:2023}. There is debate about the appropriate treatment here. \citet{Linial:2017} indexes the $\mathcal{L}_2$ rate to the $\mathcal{L}_1$ rate via an efficiency parameter. \citet{Li:2013} considers the case analogous to $\mathcal{L}_1$ outflow, where material outflowing from $\mathcal{L}_2$ acts to retard the angular momentum. In their notation $\dv{J}{t}=-\epsilon\dot{M}_1 a_{L2}^2\omega$ (compare $\dv{J}{t}=-\beta\dot{M}_1 a_{L1}^2\omega$ for \gls{rlof} through $\mathcal{L}_1$) for controllable parameters $\beta$ and $\epsilon$. The ambiguity of treatment leads to some codes to terminate at the onset of $\mathcal{L}_2$ outflow, such as \codename{Posydon} \citep{Fragos:2023}. $\mathcal{L}_2$ outflow requires stars to be in reasonably tight orbits ($\sim\SI{2}{\day})$, which we will approach, but none of our stars will enter this regime, and thus we do not afford it special treatment.

\subsubsection{Accretion}

Our wind accretion and \gls{rlof} accretion rates are taken from \citet{Bondi:1944, Hurley:2002}, and limits the accretion rate by the thermal timescale of the star. The prescription is that

\begin{equation}
    \dot{M}_\text{acc} = \min\left(\dot{M}_\text{RLOF}, \frac{M}{T_\text{KH}}\max\left(1-\frac{\omega}{0.8\omega_\text{crit}}, 0\right)\right),
\end{equation}

where $\omega$ and $\omega_\text{crit}$ are the spin rate and critical spin rate of the star. We generally assume that the star is composed of individually rotating shells of variable thickness.\footnote{Also known as shellular rotation.} If $N$ is the number of meshpoints that the star is composed of (for our cases, normally $N=199$), then the moment of inertia is given by

\begin{equation}
    I = \frac25 M_\text{c}R_\text{c}^2 + \sum_{k=1}^N \frac23 \left(m_k-m_{k+1}\right)r_k^2,
\end{equation}

where $m_k$ is the mass internal to meshpoint $k$ (similar for $r_k$), and a subscript of c refers to the central meshpoint. It is assumed that the central meshpoint sits atop a solid sphere, hence $\frac25$ as its prefactor.

\subsubsection{Bondi-Hoyle wind accretion}

Bondi-Hoyle accretion for a circular orbit is given by

\begin{equation}
\dot{M}_\text{Bondi} = \min\left[-\left(\frac{GM_2}{v_\text{W}^2}\right)^2 \frac{\alpha_\text{W}}{2a^2}\frac{1}{\left(1+v^2\right)^{3/2}}, 0.8\right]\dot{M}_\text{wind},\label{eq:bondi}
\end{equation}

where we have assumed that the orbital velocity is given by

\begin{equation}
    v^2 = \frac{G\left(M_1+M_2\right)}{a v_\text{W}^2},
\end{equation}

and that the wind velocity is given by

\begin{equation}
    v_\text{W} = 2\beta_\text{W}\frac{GM_1}{R_1}.
\end{equation}

Generalising this to an eccentric orbit causes the first argument in \Cref{eq:bondi} to pick up a coefficient of $\frac{1}{\sqrt{1-e^2}}$. There are two free parameters to select: $\alpha_\text{W}$ and $\beta_\text{W}$. These ostensibly depend on stellar types, though we select for simplicity $\alpha_\text{W}=\frac32$ \citep{Boffin:1988} and $\beta_\text{W}=7$ \citep{Lamers:1995} for O-type stars. This parameterisation is likely incomplete; \citet{Hurley:2002} argues that as per \citet{Lamers:1995}, $\beta_\text{W}$ should vary between 7 for O-type stars to 0.5 for A and F-type stars, perhaps descending as low as $\beta_\text{W}\approx\frac18$ for cool supergiants. We leave this implementation for future work under the assumption that our mass-loss rate is correct to within an order of magnitude. % concern.jpeg

\section{Post-Supernova}\label{sec:post_SN}

\mediaepigraph{Wit nodded.\\
``Oh, is this the place where I talk?'' Kaladin said.\\
``If you wish.''\\
``I don’t wish. Get on with the story.''}{Rhythm of War}{Brandon Sanderson}

After a supernova, the star leaves behind its core as a compact remnant. Generally, these are either black holes or neutron stars. The interior of a neutron star is reasonably complex, with superfluid neutrons within the crust \citep{Lattimer:2001} and a latticed core-crust interface \citep{Rueda:2014}, with ``pasta phases'' at the interface \citep{Zdunik:2017}. The interior of a black hole is impossible to observationally probe without violating causality.

The supernova ejects some fraction of material $M_\text{ej}$, leaving behind a remnant of mass $M_\text{rem}$. \citet{Eldridge:2004} gives a relationship between ejecta, remnant, and stellar masses by assuming a supernova releases \SI[print-unity-mantissa=false]{1e46}{\joule} of energy, of which 1~per~cent is transferred into the envelope. The full relation between the binding energy $U$, the stellar mass $M_*$, and the ejecta and remnant masses is thus

\begin{equation}
    \int_{M_\text{rem}}^{M_*} \left(U-\frac{GM}{r(M)}\right)\dd{M} = \SI[print-unity-mantissa=false]{1e44}{\joule},
\end{equation}

where $r(M)$ is the radius of the sphere containing mass $M$ of the star.

The orbital motions of a binary system post-secondary supernova are now deterministic from four initial parameters: $m_1, m_2, a$, and $e$. Given their high masses and small radii, we may approximate them as point masses, and thus their motion is now wholly determined by the emission of gravitational radiation through (detectable) gravitational waves. The pertinent equations of motion are given by \citet{Peters:1964}, namely,

\begin{align}
    \dv{a}{t} &= \frac{-\beta}{a^3\left(1-e^2\right)^\frac72}\left(1+\frac{73}{24}e^2+\frac{37}{96}e^4\right), \label{eq:dadt}\\
    \dv{e}{t} &= -\frac{19}{12}\frac{\beta}{a^4\left(1-e^2\right)^\frac52}\left(e+\frac{121}{304}e^3\right),\label{eq:dedt}
\end{align}

with the constant $\beta$ defined as

\begin{equation}
    \beta \equiv \frac{64}{5}\frac{G^3mM\pqty{m+M}}{c^5}.
\end{equation}

Now if one defines the constant $c_0$ through

\begin{equation}
    c_0 = a_0\pqty{1-e_0^2}e_0^{-\frac{12}{19}}\pqty{1+\frac{121}{304}e_0^2}^{-\frac{870}{2299}},
\end{equation}

then the time from the second supernova to the conclusion of the inspiral, termed the \textit{coalescence time}, will be given by

\begin{equation}
    T_\text{c}(a_0, e_0) = \frac{12}{19}\frac{c_0^4}{\beta}\int_0^{e_0} \frac{e^\frac{22}{19}\left(1+\frac{121}{304}e^2\right)^\frac{1181}{2299}}{\left(1-e^2\right)^\frac32}{ \mathrm{d}e}.\label{eq:coalescence_time}
\end{equation}

The derivations for \Cref{eq:dadt,eq:dedt,eq:coalescence_time} are given in \Cref{chap:additional_derivations}. The integral has no closed-form solution, and a numerical integrator is typically used to compute the coalescence time. However, some approximations such as \citet{Mandel:2021a} do exist and approximate the coalescence time rather well over the entire range of mass ratios.

\subsection{Merger rates of compact binaries}

The merger rate of a population of compact binaries at time $t$ after formation can be formalised via the heuristic

\begin{equation}
    m(t) = \text{number of binaries for which $T_\text{c} + T_\text{rej} + T_\text{evo} < t$},\label{eq:definition_of_merger_rate}
\end{equation}

where $T_\text{c}$ is the coalescence time, $T_\text{rej}$ is the rejuvenation age (the age at which the primary star last accreted mass), and $T_\text{evo}$ is the evolution time-scale (the length of time the primary spends on the \gls{ms}). $m(t)$ is called the \gls{dtd} of a population.

However, the underlying assumption in this is that the population was formed after an instantaneous burst of star-forming activity at time $t=0$, which is clearly unphysical. We must account for the so-called \gls{sfrd} $\psi(z)$, assumed to be taken from \citet{Madau:2014}:

\begin{equation}
    \psi(z) = 0.015\frac{(1+z)^{2.7}}{1+\left(\frac{1+z}{2.9}\right)^{5.6}}~\unit{\msun\per\year\per\mega\pc\cubed},
\end{equation}

where $z$ is the redshift. For the purposes of converting between lookback time $t$ and redshift $z$, we assume a standard $\Lambda$CDM cosmology with $\Omega_\text{M}=0.3, \Omega_\Lambda=0.7, \Omega_k = 0, H_0=\SI{70}{\km\per\second\per\mega\pc}$.

Of course, this ignores the effect of mixed-metallicity models, which we account for using the presciption of \citet{Langer:2006}, whereupon the \gls{sfrd} is multiplied by a factor $\chi$:

\begin{equation}
    \chi\left(z,Z\right) = \frac{\hat{\Gamma}\left[0.84, \left(\frac{Z}{Z_\odot}\right)^2\cdot 10^{0.3z}\right]}{\Gamma\left(0.84\right)},
\end{equation}

where $\hat{\Gamma}$ and $\Gamma$ are the upper incomplete and complete Gamma functions respectively. This means our \gls{sfh} -- which takes into account both redshift and metallicity -- denoted $\Psi(z,Z)$, is given by

\begin{equation}
    \Psi(z,Z) = 0.015\int_{z_1}^{z_2} \frac{(1+z)^{2.7}}{1+\left(\frac{1+z}{2.9}\right)^{5.6}}\frac{\hat{\Gamma}\left[0.84, \left(\frac{Z}{Z_\odot}\right)^2\cdot 10^{0.3z}\right]}{\Gamma\left(0.84\right)}~\mathrm{d}z,
\end{equation}

where $z_1$ and $z_2$ are the redshifts of our bin edges. The event rate as a function of time, $\mathcal{R}(t)$, is therefore given by the convolution

\begin{equation}
    \mathcal{R}(t) = \Psi(z_t) * m(t),\label{eq:event_rate_distribution}
\end{equation}

where $z_t$ is the redshift corresponding to lookback time $t$, given by the solution to the equation

\begin{equation}
    t = t_\text{H} \int_0^{z_t} \frac{\dd{z^\prime}}{\pqty{1+z^\prime}\sqrt{\Omega_\text{M}\pqty{1+z^\prime}^3+\Omega_\text{k}\pqty{1+z^\prime}^2+\Omega_\Lambda}},
\end{equation}

with $t_\text{H}$ the Hubble time, and $\Omega_\text{M}, \Omega_\text{k}, \Omega_\Lambda$ the matter density, curvature parameter, and cosmological constant density parameter of the Universe. In general this integral does not have a closed-form solution, however in the case of a flat Universe with $\Omega_\text{k}=0$, an exact solution for redshift\footnote{Integral was computed via \codename{Sympy}.} is

\begin{equation}
z = \pqty{\frac{a}{\sinh\pqty{\operatorname{asinh}{\left(a \right)}+\frac{t}{b}}}}^\frac23 - 1,
\end{equation}

where $a=\sqrt{\frac{\Omega_\Lambda}{\Omega_\text{M}}}$, and $b=\frac{1}{H_0}\frac{2}{3\sqrt{\Omega_\text{M}}}$. Detail on how the convolution in \Cref{eq:event_rate_distribution} is computed may be found in \citet{Richards:2021}.

\section{Numerical implementation}

\epigraph{I think there is a world market for maybe five computers.}{Thomas Watson, President of IBM, 1943}

Having now discussed the evolution of stars from a theoretical perspective, it becomes imperative to discuss the methods of modelling them. In this section, we provide an overview of the four codes we either use extensively or have developed to carry out the work of this thesis. These four codes are:

\begin{enumerate}
    \item The Cambridge \codename{stars} code (\Cref{chap:numerical_methods_stars}), originally developed by Peter Eggleton, which we use to solve the equations of stellar structure and evolution,
    \item The \codename{Tui} code (\Cref{chap:numerical_methods_tui}), developed by Jan Eldridge, which performs our binary population synthesis of gravitational wave sources,
    \item The \codename{Kaitiaki} code (\Cref{chap:numerical_methods_kaitiaki}), developed for this thesis, which is our grid-manager code and allows modification of the input parameters to the Cambridge \codename{stars} code programmatically, and
    \item The \codename{Takahe} code (\Cref{chap:numerical_methods_takahe}), developed by Sean Richards for \citet{Richards:2021}, which evolves the orbits of our binary compact objects.
\end{enumerate}

\subsection{The Cambridge \codename{stars} code}\label{chap:numerical_methods_stars}

\subsubsection{Brief History}

\mediaepigraph{And through the night, behind the wheel, the mileage clicking west\\I think upon Mackenzie, David Thompson and the rest\\Who cracked the mountain ramparts and did show a path for me\\To race the roaring Fraser to the sea.}{Northwest Passage}{Stan Rogers}

In 1959, Louis George Henyey published his method for automatic stellar evolution \citep{Henyey:1959}. Prior to this -- and the widespread availability of the computer -- a model was treated as a mathematical process and thus was computed, often labouriously, by hand. For instance, in \citet{Eggleton:1967}, Eggleton writes that

\begin{fanciful}
    Since we have seen in Section 3 that the structure of the flame zone can be obtained independently (almost) of the rest of the star, it is reasonable to expect that [thermal] instabilities of the type considered by Schwarzchild \& H\"arm can also be examined in this way, so that the considerable labour of computing the structure of an entire star can be avoided.
    \begin{flushright}
        -- \citet{Eggleton:1967}
    \end{flushright}
\end{fanciful}

The Cambridge \codename{stars} code was one of the earliest detailed stellar evolution codes to be written, though initially it only modelled one star in detail. To model a binary system, tweaks were made whereupon the primary would be modelled in detail whilst approximating the secondary with the equations of \citet{Hurley:2002} until the primary supernova, at which point the secondary would be evolved in detail. This had its merits; notably computation time was saved, and the results were generally accurate anyway.

However, \citet{Stancliffe:2009} introduced detailed modelling of \textit{both} stars in a binary system into the \codename{stars} code -- that is, the interiors of both stars were computed in detail, alongside the simualtaneous computation of the orbital parameters. Since then, a wide array of improvements have been made to the numerical stability of the code, as well as introducing or refining new physics.

There are four variants of this code:

\begin{enumerate}
    \item The Cambridge \codename{stars} code, which is the version originally developed by Peter Eggleton and originally described in \citet{Eggleton:1971}. This version was originally only capable of solving the equations for a single star.
    \item The \codename{Eldridge stars} code described in \citet{Eldridge:2004, Eldridge:2004a, Eldridge:2008, Eldridge:2017}, which is the version used to generate the fiducial \gls{bpass} v2 grids. This version models the primary in detail while approximating the secondary using the equations of \citet{Hurley:2002}.
    \item The \codename{bstars} code, which is the unmodified version lately available from Cambridge, described in \citet{Stancliffe:2009}. This version models both stars in detail throughout their lives.
    \item The \codename{Aotearoa stars} code, which is the version of the code incorporating the changes we have made.
\end{enumerate}

Unless context otherwise requires, when we refer to ``the \codename{stars} code'' with no further qualification, we are referring specifically to the \codename{Aotearoa stars} code.

\subsubsection{Matrix Inversion}

The heart of \codename{stars} is a matrix inversion algorithm. Although computing the inverse of a matrix is a polynomial time process, it can still take considerable time to invert. \codename{stars} implements a Henyey matrix \citep{Henyey:1964}, which is of size $n=(4+n_\text{comp})N_\text{mesh}$, with $n_\text{comp}$ the number of equations to be solved and $N_\text{mesh}$ the number of meshpoints in the model. An individual element of the Henyey matrix is given by

\begin{equation}
    \mat{H} = \pdv{\pqty{\text{equation } i}}{\pqty{\text{variable } j}}.
\end{equation}

For a ``typical'' binary computation, with $n_\text{comp}=30$ equations and $N_\text{mesh}=199$ meshpoints, the matrix is already $6766\times6766$, for a binary which evolves up the \gls{agb} (the theoretically most complex calculation we perform) with $N_\text{mesh}=999$, the matrix is $33966\times33966$. Introducing $k$ new equations into the code causes the runtime of the matrix inversion calculation to scale like $\mathcal{O}(n^3+k^3N_\text{mesh}^3)$.

The matrix inversion is simple Gauss-Jordan inversion, which is of complexity class (for an $n\times n$ matrix) $\mathcal{O}(n^3)$. \citet{Coppersmith:1987} showed that this could be optimised to $\mathcal{O}(n^{2.376})$ and conjectured reducibility to $\mathcal{O}(n^2)$.\footnote{The current ``best'' optimisation is $\mathcal{O}(n^{2.3727})$ \citep{Williams:2012}. DeepMind's AlphaTensor has claimed a faster result in \citet{Fawzi:2022}, though they do not state a complexity class. This is because ``...this does not yield practical algorithms.''} Since our code is embarassingly parallelisable, and can be executed in parallel simply by increasing the number of cores available, the payoff for decreasing the runtime by one polynomial order is inconsequential, and thus we use the straightforward implementation.

\subsubsection{Initial Condition Selection}

In this section we provide an account for the selection of our general initial conditions. Unless otherwise noted in this thesis, it is assumed that the values will be using the parameters described here and in \Cref{chap:param_config}.

In our implementation of \gls{mlt} \citep{Vitense:1953, Bohm-vitense:1958, Cox:1968}, we use an \gls{mlt} parameter of $\alpha_\text{\gls{mlt}}=1.985$, selected via calibration against a model of the Sun. This is effectively a dissipativity parameter, and represents the depth at which a parcel of matter penetrates into a lower layer before becoming fully dissipated. The value was selected by modelling a \SI{1}{\msun} star and varying only the mixing length parameter until it had a lumonosity of \SI{1}{\lsun} and a radius of \SI{1}{\rsun}, at the present age of the Sun.

\subsubsection{Mesh spacing}

A natural question that arises when modelling a star is where internally to model it -- there is a counterbalance between the level of detail and the time cost of the matrix inversion. The obvious answer would be to distribute a mesh equally spaced within radius of the star, however the nonhomogeniety of a star, plus the potential for very thin shells of physically important material, necessitate a more intelligent approach.

Instead of being equally spaced \textit{radially}, these mesh points are taken to be equally spaced within a function $\mathcal{Q}$, which is taken from \citet{Stancliffe:2004} and given for a binary that does not have \gls{agb}-specific treatment enabled by

\begin{align}
    \mathcal{Q}\left(P, T, M, R\right) &= c_4\ln P + c_5\ln\left(P+c_\text{H}\right) + c_2\ln\left(P+3 P_\text{He}\right) + c_2\ln\left(P+0.3 P_\text{He}\right) + c_7\ln\left(\frac{T}{T+2\times 10^4}\right) \notag\\
    &\phantom{=} + \ln\left(\frac{c_6M^\frac23}{M + M^\frac23}\right) - c_3\ln\left(\frac{R^2}{c_8}+1\right),
\end{align}

where $P, T, M, R$ are the total pressures, temperatures, masses, and radii of each star, the constants $c_i$ are user controllable and given in \Cref{chap:param_config}, $P_\text{He}$ is the pressure in the helium-burning shell, and

\begin{equation}
    c_\text{H} = \begin{cases}
        0.1 P_\text{H} & X_\text{H} \geq 0.15 \\
        0.1 \mathcal{P} & \text{otherwise}
    \end{cases}
\end{equation}

where $P_\text{H}$ is the pressures in the hydrogen-burning shell. Here, $\mathcal{P}$ represents the pressure in the hydrogen-burning shell when $X_\text{H} = 0.15$, and its inclusion obviates numerical issues if the envelope is removed and there is no hydrogen burning occuring. The choice of mesh spacing coefficients $c_1\dots c_{10}$ leads to different areas of the star being prioritised. Coordinates $c_1, c_9,$ and $c_{10}$ are not used except in \gls{agb}-specific evolution, which is not required for most cases (and requires a change of parameterisation in the pressure-shell terms). During binary evolution, $c_5$ is hardcoded to be 0.15, and $c_2$ to be 0.0, to focus meshpoints into the hydrogen-burning shell.

Placement of the mesh is controlled by the derivative $\pdv{\mathcal{Q}}{k}$, where $k$ is the meshpoint number \citep{Stancliffe:2016}. Specifically, meshpoints are placed at locations where

\begin{equation}
    \eval{\pdv{\mathcal{Q}}{k}}{k}-\eval{\pdv{\mathcal{Q}}{k}}{k+1} = 0.
\end{equation}

The mesh is thus adaptive (insofar as it changes to place meshpoints where they are needed for evolution), non-Lagrangian (insofar as the derivative is not at constant mass coordinate), and inviscid (insofar as the gradient of $\mathcal{Q}$ is constant through meshpoint number). This combination of requirements is sometimes referred to as an Eggletonian mesh. If one desires to make the mesh viscous, one can follow \citet{Stancliffe:2004} and write, for $0\leq\gamma\leq1$,

\begin{equation}
    \pqty{\eval{\pdv{\mathcal{Q}}{k}}{k}-\eval{\pdv{\mathcal{Q}}{k}}{k+1}}\pqty{1-\gamma} + \gamma\pqty{\eval{\pdv{m}{t}}{k}} = 0,
\end{equation}

where now the parameter $\gamma$ is a weighting coefficient such that if $\gamma=1$, then the mesh spacing function is irrelevant and the parameters are spaced equally in mass, and if $\gamma=0$, the original mesh spacing function is used. In between, if $\gamma=\gamma\pqty{k, \dd{t}}$, then parts of the mesh are allowed to be Lagrangian and parts non-Lagrangian. We typically keep the mesh non-viscous and non-Lagrangian, as a viscous mesh is most useful for \gls{agb} evolution and evolving through thermal pulses.

More detail is provided in \citet{Eggleton:1971} for the original mesh spacing function, and \citet{Stancliffe:2004} for the binary mesh spacing function.

\subsubsection{Timestep control}

The \codename{STARS} code utilises an adaptive timestep control mechanism, whereby the timestep is allowed to shrink to model rapid changes or grow if the star is broadly inert and unchanging. The core computation is a parameter $\Delta$, given by

\begin{equation}
    \Delta = \frac{d_\text{opt}}{\sum_i \sum_j \left|\delta x_{ij}\right|},\label{eq:timestep_selection}
\end{equation}

where $d_\text{opt}$ is a pre-defined target timestep (typically taken to be $\sim 1$, unless otherwise specified; see \Cref{chap:param_config} for our selections), and $\delta x_{ij}$ is the change in variable $x_i$ at meshpoint $j$. In this calculation, luminosity is explicity excluded as this would cause the timestep to explode during certain points of the star's life-time. A new timestep, $d^\prime$, is selected from the previous timestep $d$ by

\begin{equation}
    d^\prime = \begin{cases}
        \min\left[d_1, \Delta\right]d & \Delta < 1 \\
        d & \Delta = 1 \\
        \max\left[d_2, \Delta\right]d & \Delta > 1
    \end{cases},
\end{equation}

where $d_1$ and $d_2$ are pre-determined control parameters. Most commonly we take $d_1=0.1$ and $d_2=1.2$ unless otherwise specified.

\subsubsection{Numerical instabilities}\label{sec:numerical_instabilities}

Stellar modelling is a nontrivial endeavour and there are many cases where the code can fail seemingly arbitrarily. The largest cause of numerical instability is the helium flash, which occurs in low-mass stars between about $\SI{0.8}{\msun} \lesssim M_\text{ZAMS} \lesssim \SI{2.5}{\msun}$. In this band, the triple-$\alpha$ process occurs within a highly electron-degenerate helium core. As a result, the temperature of the star increases sharply during this process, leading to thermal runaway. This temperature increase in turn results in an increase in the luminosity (which peaks at $\sim\SI{1e10}{\lsun}$). The flash itself -- being the period of thermal runaway -- occurs on the order of a few minutes, though the spike in luminosity lasts approximately $\SI{7}{\mega\year}$ (for the $\SI{2.51}{\msun}$ star shown in \Cref{fig:thermal_pulses_he_flash}).

A method termed ``pseudoevolution'' can be used to overcome this source of error. Briefly, a complete model (termed the \textit{reference model}) of a \SI{3}{\msun} star is evolved with a full model description saved at every timestep. When the helium flash occurs in a model (heuristically, $0.1 \leq M_\text{He} / \unit{\msun} \leq 0.55$ and $\log T_\text{c} / \unit{\kelvin} > 7.9$), we query the reference model for a model with the same helium core mass as the failed model. Using a custom subroutine in \codename{stars}, we turn off all nuclear and thermal burning and remove mass from the reference model to match the mass of the failed model. This model is then allowed to evolve as per normal, having overcome the helium flash. We note that this is not a \textit{perfect} method, as this does induce discontinuities in the plots, as shown in \Cref{fig:pseudoevolved_kippenhahn}, though these are essentially insignificant.

The other stage where low-mass stars are prone to fail is during a thermal pulse \citep{Schwarzschild:1965, Schwarzschild:1967}. The mass range for thermal pulses is broader, from $\sim\SI{0.8}{\msun}$ to $\sim\SI{8}{\msun}$. A thermal pulse occurs as a star ascends the \gls{agb}; as a \gls{CO_core} or \gls{ONe_core} has developed, shells of burning helium and hydrogen (separated by a region of almost pure helium) sit atop the core \citep{Pols:2001}. As the intershell region dissipates due to dredge up or helium burning, hydrogen burning reignites in the hydrogen shell. As both shells burn (at the same rate), the helium-burning shell becomes unstable and eventually a process of thermal runaway begins (similar to the helium flash, except off-core). This causes a spike in the luminosity, termed a thermal pulse. This can persist for many pulses -- \citet{Stancliffe:2006} observes pulses before model termination, while \citet{Stancliffe:2005} observes 17 pulses for a model similar to those in \Cref{fig:thermal_pulses_he_flash}.

The method to overcome this numerical instability is involved and not fully implemented in \codename{STARS}. Briefly, the number of meshpoints must be drastically increases near the onset of thermal pulses; up to 999 instead of the typical 199/499. The timestep must also be forcibly decreased; thermal pulses typically last for around a year \citep{Pols:2001} so the timestep must be reduced to not miss them (and to not have the modulus of the change in parameters -- $\sum_i \sum_j \left|\delta x_{ij}\right|$ in \Cref{eq:timestep_selection} -- and thus the error, increase too much). This method was implemented to generate \Cref{fig:thermal_pulses_he_flash}, though only two pulses were achievable.

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{images/TheorySection/thermal_pulses_and_he_flash.pdf}
    \caption[Two stars of a similar mass that undergo a helium flash and two thermal pulses.]{Two stars of a similar mass that undergo a helium flash and two thermal pulses. Shown in the inset is a zoomed-in helium flash that lasts $\sim\SI{7}{\mega\year}$. Both stars undergo two thermal pulses before the code crashes due to the relative error in the calculation increasing too rapidly. The two stars could, in principle, undergo many more thermal pulses before their deaths.}
    \label{fig:thermal_pulses_he_flash}
\end{figure}

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{images/TheorySection/helium_flash_kippenhahn.pdf}
    \caption[The helium flash shown on a Kippenhahn diagram.]{The helium flash (gray line) shown on a Kippenhahn diagram. The total mass is shown as the solid gray line, and the \acrshort{CO_core} mass is shown in red. The green lines show the boundaries of the convective regions of the star.}
    \label{fig:pseudoevolved_kippenhahn}
\end{figure}

\subsubsection{Critical modifications}

The major modifications we have made to the \codename{stars} code are as follows.

Firstly, the floating-point precision has been increased to double precision for all variables. This has minor effects on the evolved models, insofar as they are generally able to evolve to slightly further stages. This step has previously been undertaken, however not all variables were correctly converted. This did not invalidate any models previous versions of the \codename{stars} code has generated; rather it means they terminated slightly earlier than the point to which they may have been able to evolve.

As part of this, we also undertook an extensive refactor of the codebase. Whilst some of the code remains as originally written, it is now more compliant with modern programming practices and is in some sense easier to alter with new routines.

We have also improved mass loss interpolation in the \codename{massloss.f} routine. Whilst not changing the underlying assumption that mass transfer is governed by \citet{De_jager:1988, Vink:2001} (\Cref{sec:mass_transfer}), we have improved the transition between typical O-type mass loss and WR mass loss rates by slowing turning on the WR mass loss rate as the surface hydrogen abundance drops below 0.4. The transition was previously abrupt and would cause the code to fail due to numerical instability if a non-WR star entered this regime.

Similarly to this, the code now manually reduces the timestep in the run-up to, during, and after the contact binary phase. Evolution during this phase breaks thermal equilibrium (i.e., the star evolves on a longer timescale than its Kelvin-Helmholtz time-scale) and hence the evolution can change rapidly as the star attempts to adjust back to thermal equilibrium. The contact phase, for example, typically last for between 100-200 \unit{\kilo\year} in our models.

We have also begun implementing an $\alpha-\lambda$ treatment of \gls{cee}, though we stress that this has not been rigorously tested yet, and is thus disabled for the majority of our models.

\subsection{The \codename{Tui} code}\label{chap:numerical_methods_tui}

\codename{Tui} is the core population synthesis code within the \gls{bpass} project. \codename{Tui} takes as input the output from the \codename{stars} code. \codename{Tui} was first described in \citet{Ghodla:2023}, and was further described in \citet{Stevance:2023}, though we note that their version is technically a refactor of the version we use. The predominant function we use \codename{Tui} for is the matching of pre- and post- supernova models for \Cref{chap:natal_kicks}, and in this section we provide a brief account of that functionality of \codename{Tui} (though the control of the population synthesis models is beyond the scope of this thesis).

\codename{Tui} has precompiled ``input\_'' files which report a \codename{STARS} plot file, an \gls{imf} weight, and a model type (merger/normal primary/normal secondary/single model/quasi-homogeneous evolution binary/single star). After loading in a model, \codename{Tui} applies a given kick and computes the new orbital parameters from \citet{Tauris:1998}. Crucially, a core assumption of \gls{bpass} is that the orbit remains circular after the first supernova and is allowed to be eccentric after the second. Of course, nature cares little for our assumptions, and the supernova may cause the binary to become eccentric after the first supernova; \codename{Tui} accounts for this by setting the separation after the first supernova to be the semi-latus rectum of the eccentric binary. \codename{Tui} repeats this process after the second supernova.

For the purposes of \Cref{chap:natal_kicks}, if the binary remains bound (has $0\leq e < 1$) after the second supernova, we record the following parameters:

\begin{enumerate}
    \item The mass of the primary,
    \item The mass of the secondary,
    \item The orbital separation,
    \item The eccentricity,
    \item A weight term proportional to the \gls{imf} weighting,
    \item The rejuvenation age, which is the time since the companion last accreted material, if it accreted more than five per cent of its initial mass \citep[see][]{Richards:2023}, and
    \item The evolution age, which is the length of time from the secondary's \gls{zams} until its death.
\end{enumerate}

This provides us with the post-secondary supernova orbital parameters which determine the properties of the merger, such as the event rate $\mathcal{R}(z)$ and its path through period-eccentricity space.

\subsection{The \codename{Kaitiaki} grid-watcher code}\label{chap:numerical_methods_kaitiaki}

\codename{Kaitiaki} is a grid-watcher code we have developed that sits atop the \codename{STARS} code and manages its execution. It was developed primarily to manipulate the fixed-width text files that \codename{STARS} uses, in contrast to the ``typical'' manipulation done through the command line. However development proceeded in such a way that it now provides an interface to all of the (relevant to this thesis) output and input files. Further, it also now allows the user to control jobs through a \textsc{Python} interface, greatly simplifying grid calculations.

The core of \codename{Kaitiaki} is the \mintinline{Python}{STARSController()} interface. The most na\"ive run would be to initialise a model of, say, \SI{8}{\msun} and evolve it until it evolves up the \gls{agb}. A sample of code for this is shown in \Cref{kaitiaki_runs} which evolves a \SI{10}{\msun} star.

\pythonfile[label=kaitiaki_runs]{A \codename{Kaitiaki} script to evolve a \SI{10}{\msun} star}{chapters/listings/kaitiaki_demo.py}

\section{Chapter Summary}

\epigraph{If in the first act you have hung a pistol on the wall, then in the following one it should be fired. Otherwise don't put it there.}{Anton Chekhov}

In this Chapter, we have explored the evolution of stars from pre-\gls{ms} evolution to supernova, accounting for the effects of binary interaction. This represents the state of the field as at the commencement of this work. In future chapters we will detail the relevant changes we have made to these prescriptions, motivating the work we have performed.